# 配置目录

主责：计算机软件同学。项目负责人维护公共配置 Schema 和早期 Mock 样例。

- `mock/`：仅用于接口联调，所有文件必须明确标记 `MOCK`；
- 正式运行配置须关联 Schema、提交、随机种子和结果索引；
- 模块算法参数由对应模块同学确认，软件同学不得自行改动；
- Mock 数据不是仿真结果，不得进入阶段验收。

当前状态：目录结构已规划，Mock 契约样例已建立；正式配置管理尚未开始。
