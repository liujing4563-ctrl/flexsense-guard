# App 工作包

主责：计算机软件同学。

负责配置、状态展示、曲线、结果索引、错误处理和演示集成。P1 通过前只允许使用
`configs/mock/**` 验证接口，不开发完整 App，不把 Mock 展示当作实验成果。

子目录：

- `dashboard/`：看板和曲线；
- `state_manager/`：前端状态、加载和错误处理；
- `demo/`：可复现演示入口。

当前状态：目录结构已规划，主体实现 `BLOCKED`。
