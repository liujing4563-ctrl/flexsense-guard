# 历史技术探针执行记录

## 当前解释状态

| 层级 | 状态 |
|---|---|
| 历史 P1 执行结果 | `FAIL` |
| 历史证据有效性 | `INVALID / INSUFFICIENT` |
| 当前 P1 可行性结论 | `NOT_VERIFIED` |
| P2 | `BLOCKED` |
| P3 | `BLOCKED` |

旧 P1 实验存在力矩侧定义未冻结、Plant 与 Observer 输入不一致、单负载、单场景、
单随机种子以及同批真值选择 Q 等问题。因此，本文件保留“旧脚本曾运行失败”的
事实，但不再用于认定负载侧状态不可估计或 EKF 技术路线不可行。

## 执行环境

- 运行环境：MATLAB R2024a。
- 场景：`nominal_accel_decel`，采样时间 0.001 s，持续时间 10 s。
- 随机种子：42。
- 估计器输入：仅电机侧位置、速度和力矩；负载侧真值仅在离线指标计算时读取。

## P1：负载侧状态估计

预先定义的过程噪声尺度为 `1`、`1e-2`、`1e-4`、`1e-6` 与 `1e-8`。验收需要
同时满足：负载侧位置 RMSE 小于 0.05 rad，且相较电机侧直接映射至少改善 30%。

| 指标 | 结果 |
|---|---:|
| 电机侧直接映射 RMSE | 0.002584 rad |
| 最佳 EKF RMSE | 3.626873 rad |
| 最佳过程噪声尺度 | `1e-8` |
| 相对改善 | -140248.73% |
| 历史 P1 执行结论 | `FAIL` |

结果只说明旧实现和旧实验配置未通过当时门限。由于证据基础存在确定性缺陷，不能
据此判断修正后的增广 EKF 是否优于电机侧直接映射。

## 阶段门决定

- P2（外扰区分）和 P3（可信评分）**未运行**：当前 P1 为 `NOT_VERIFIED`，继续阻断。
- 不得基于旧草稿的打印信息，声明外部接触识别、可信评分有效或安全控制有效。
- 旧记录中的“降级为电机侧控制仿真”不再作为当前路线决定。当前路线以项目章程
  为准：先冻结模型和输入契约，再由 Simulink 同学修复并重新运行 P1；只有基于
  有效证据仍失败时，才进入 C 路线。
