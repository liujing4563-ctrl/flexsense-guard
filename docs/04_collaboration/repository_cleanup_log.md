# 仓库历史风险记录

## 记录目的

本文只登记当前主线之外的历史残留及迁移风险，不执行 stash、分支或 Pull Request
清理操作。记录日期为 2026-07-21，当前权威基线为 `main`。

## 已知历史残留

| 对象 | 当前风险 | 处理原则 |
|---|---|---|
| GitHub PR #9 | 基于旧主分支，包含大范围旧目录和实现，不能直接合并 | 保持原状，后续逐文件审计 |
| `stash@{0}` | 可能包含旧 Plant、EKF、分类和旧文档结构 | 禁止整体恢复，只允许逐文件查看和选择性迁移 |
| `stash@{1}` | 少量旧 README 修改，语义可能已过期 | 保持原状，确有需要时单独比较 |
| 旧本地及远端分支 | 可能包含已替代的接口、枚举和阶段结论 | 不删除；迁移前与当前 `main` 逐文件比较 |

## 迁移约束

任何历史代码或文档迁移都必须遵循当前权威架构、接口和阶段状态，不得重新引入：

- 含义不清的 `tau_m`、`tau_s` 跨模块公共语义；
- 小写分类枚举；
- 小写运行模式枚举；
- 把历史 P1 `FAIL` 当作当前可行性结论的表述；
- 把 `00_docs/` 或旧平铺路径重新设为权威文档；
- Observer 直接使用 `torque_command_nm` 或负载侧真值的链路。

## 明确禁止的清理操作

本轮不执行 `git stash pop`、`git stash apply`、`git branch -D`、
`git push --delete` 或关闭 PR #9。后续若需清理，必须建立独立任务、先生成差异清单，
再由项目负责人确认处理范围。

