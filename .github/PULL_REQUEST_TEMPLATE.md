## 本次唯一目标

<!-- 说明本 PR 解决的一个阶段性问题。 -->

## 范围与非目标

- 允许修改的模块：
- 明确不修改的内容：
- 负责同学：
- 交接对象：

## 变更摘要

-

## 公共接口检查

- [ ] 没有修改 Issue 允许范围之外或非本模块的主体代码
- [ ] 文档、Schema 和语言类型已同步
- [ ] 未引入 `contact_probability`
- [ ] 外扰输出统一为 `estimated_external_torque_nm`
- [ ] 未在缺少 Jacobian 或等效力臂时声称末端力
- [ ] 未将负载侧真值输入运行时算法
- [ ] Plant 真实参数与 Observer 名义参数保持分离
- [ ] 力矩命令、实际施加力矩和测量力矩已分离
- [ ] Observer 未直接使用原始 `torque_command_nm`
- [ ] 历史执行结果、证据有效性和当前技术结论已分层记录

### P1 模型修复检查

- [ ] 已明确力矩所在侧
- [ ] 已检查齿轮比方向
- [ ] Plant 和 Observer 使用一致的物理输入链
- [ ] 未将 `torque_command_nm` 直接作为实际力矩
- [ ] 未使用负载侧真值作为 Observer 输入
- [ ] Plant 真实参数与 Observer 名义参数分离
- [ ] 未使用测试真值选择 Q/R/P
- [ ] 未降低 P1 门禁
- [ ] MATLAB 测试已实际运行，或明确标记 `NOT RUN`
- [ ] 未修改 P1 历史结果
- [ ] 未启动 P2/P3

## 阶段门与证据

- 当前阶段门：
- 本 PR 是否改变阶段决定：
- 配置、随机种子和结果位置：
- 失败记录是否完整保留：

- [ ] 没有降低阶段门或在评价集上调整参数
- [ ] 没有删除、覆盖或静默排除失败案例

## 验证

- [ ] `python -m pip install -e .`
- [ ] `python -m pytest -q`
- [ ] MATLAB/Octave：`PASSED` / `FAILED` / `NOT RUN`
- [ ] C/SIL：`PASSED` / `FAILED` / `NOT RUN`
- [ ] App：`PASSED` / `FAILED` / `NOT RUN`
- [ ] 所有勾选为通过的命令均已真实运行，未运行项明确标记 `NOT RUN`

实际执行命令与结果：

```text

```

## 风险与降级

- 已知风险：
- 触发降级条件：
- 推荐路线：

## 责任与发布声明

- [ ] 对应模块负责同学已确认主体实现
- [ ] 未把 Codex 生成代码错误归为提示词发起人的个人模块成果
- [ ] 未包含专利、比赛敏感材料或个人数据
- [ ] 未虚构实验、硬件、实时性或工业安全结果
- [ ] 未启用自动合并
